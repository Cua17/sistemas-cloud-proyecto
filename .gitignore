# Vagrant / VirtualBox
.vagrant/
*.box

# Ansible
*.retry

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets / llaves (nunca versionar)
*.pem
*.key
*_rsa
*_rsa.pub
secrets/
*.env
credentials-minio
restic-password*

# Kubeconfig (contiene certificados y token del clúster)
infra/kubeconfig/*.yaml
infra/kubeconfig/*.yml

# Sistema
.DS_Store
Thumbs.db

# Presentaciones y PDFs pesados del curso (material de referencia, no del proyecto)
*.pdf

# Resultados generados (se copian a docs/informe/evidencias/ cuando se quieren guardar)
pruebas/resultado-conectividad.txt
